import React from 'react'; // Import React to use JSX syntax

// Define Dashboard as a functional component
const Dashboard = () => {
    return (
        <div>Dashboard</div>
    );
};

export default Dashboard;